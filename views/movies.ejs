
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movies List</title>
</head>
<body>
    <h1>Movies</h1>
    <ul>
        <% movies.forEach(function(movie) { %>
            <li>
                <a href="/movie/<%= movie.id %>"><%= movie.attributes.title %></a>
            </li>
        <% }); %>
    </ul>
</body>
</html>